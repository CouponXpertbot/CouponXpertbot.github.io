name: CouponXpert Bot

on:
  workflow_dispatch:

  schedule:
    - cron: '0 * * * *'
  
permissions:
  contents: write
  
jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        run: python bot.py
        
      - name: Commit updated posted_courses.txt
        run: |
              git config --global user.name "github-actions[bot]"
              git config --global user.email "github-actions[bot]@users.noreply.github.com"
        
              git add posted_courses.txt
      
              git diff --staged --quiet || git commit -m "Update posted courses"
        
              git push
